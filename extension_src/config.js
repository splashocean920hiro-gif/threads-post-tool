// このファイルは、ツールの「初期設定・拡張機能」からダウンロードした zip では
// あなた専用の設定（送信先URL・アクセストークン）に置き換わります。
//
// ソースから直接読み込んで使う場合は、このまま（未設定）で構いません。
// その場合の送信先とトークンは、拡張機能のポップアップで手動設定してください。
//
// PRESET を null のままにしておくと、background.js 側の
//   (self.PRESET && self.PRESET.xxx) || 既定値
// がすべて既定値に落ちるので、保存済みの設定が上書きされません。
self.PRESET = null;
